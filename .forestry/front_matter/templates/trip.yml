---
label: Trip
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: decription
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: decription
- name: step
  type: field_group_list
  fields:
  - name: step
    type: include
    config: {}
    template: step
    label: step
  config:
    min: 
    max: 
    labelField: 
  label: Step
